<div class="bckgrd_mes_commandes">
  <div class="container">
    <br>
    <h1 class="title_mes_commandes"><strong>Mes commandes</strong></h1>
    <br>
      <div class="row">
        <% @orders.where(user_id: @user).each do |order| %>
            <div class="col-md-4">
              <%= render 'order', order: order, user: @user %>
            </div>
        <% end %>
      </div>
    <br>
  </div>
</div>
