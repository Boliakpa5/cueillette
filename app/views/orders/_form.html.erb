
<%= simple_form_for([product, order]) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
  <%= f.input :pickup_date, html5: true, label: 'choisissez votre date de collecte', placeholder: 'clic sur le calendrier' %>


<br>
<div class="form-actions">
    <%= f.button :submit, 'Réserver maintenant', class:"btn btn-success" %>
  </div>
<% end %>
